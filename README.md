# jetlog-mcp

Python MCP server. Extracted from the `whitehouse-rke2` monorepo — the
Kubernetes manifests remain there and deploy the image this repo builds.

Image: `ghcr.io/robertdwhite/jetlog-mcp` (multi-arch amd64+arm64, built by CI on
push to `main`). The GitOps repo pins a specific digest.
